meta {
  name: Create Topic
  type: http
  seq: 1
}

post {
  url: {{local_path}}/topic/create
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "topicName":"OVERRIDE_TOPIC_ID"
  }
}
